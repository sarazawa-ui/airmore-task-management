# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp

# Local server
start-server.bat
start-server.ps1

# Backups
*.bak
*~

# Logs
*.log

# サービスアカウントキー（機密・絶対にコミットしない）
airmore-task-management-app-*.json
*serviceaccount*.json
*-key.json

# バックアップ処理の依存パッケージ（GitHub Actions 側で install する）
backup/node_modules/
backup/package-lock.json

# データのバックアップ(公開リポジトリのため絶対に入れない。保存先はGoogleドライブ)
backup/*.json
!backup/package.json
!backup/package-lock.json
*.backup.json
backup-*.json
hittatsu-backup-*.json
